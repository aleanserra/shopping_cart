export interface ShippingDetailProps {
  itemIndex: number;
  productName: string;
  amount: string;
  quantity: number;
}
export interface ShippingDetail {
  name: string;
  email: string;
  address: string;
  postalCode: string;
  country: string;
  phone: string;
}
