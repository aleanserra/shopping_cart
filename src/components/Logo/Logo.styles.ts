import styled from "styled-components";

export const Container = styled.div`
  h2 {
    font-size: 2rem;
    font-weight: normal;
    cursor: pointer;
    span {
      font-weight: bold;
    }
  }
`;
